<h1>トップページ</h1>

<%= link_to '新規投稿', '/posts/new' %>

<% @posts.each do |post| %>
  <div class="todo">
    <div class="todo-date" >    
      投稿日時<%= post.created_at %>
    </div>
    <div class="todo-content">
      <%= post.content %>
    </div>
  </div>
<% end %>